---
// src/components/LocalImage.astro
interface Props {
  node: {
    src: string;
    alt: string;
    caption?: string;
    href?: string;
    pinDescription?: string;
  };
}

const { node } = Astro.props;
const { src, alt, caption, href, pinDescription } = node;
---

<figure data-pin-description={pinDescription}>
  {href ? (
    <a href={href}>
      <img src={src} alt={alt} loading="lazy" decoding="async" data-pin-description={pinDescription} />
    </a>
  ) : (
    <img src={src} alt={alt} loading="lazy" decoding="async" data-pin-description={pinDescription} />
  )}
  {caption && <figcaption>{caption}</figcaption>}
</figure>
